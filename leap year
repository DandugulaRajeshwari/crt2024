int main() {
    // Write C code here
    int a;
    scanf("%d",&a);
    if(a%4==0 && (a%100!=0||a%400==0))
    {
        printf("it is leap year\n");
    }
    else
    {
    printf("it is not a leap year");
    }
    return 0;
}
