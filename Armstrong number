#include <stdio.h>

int main() {
    // Write C code here
    int n,d,s=0,t,r;
    scanf("%d",&n);
    t=n;
    while(n>0)
    {
       d=n%10;
       n=n/10;
       s=s+d*d*d;
    }
    if(s==t)
       printf("Armstrong no");
    else
    printf("Not an armstong no");
    return 0;
}
