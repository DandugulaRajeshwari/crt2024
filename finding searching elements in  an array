#include <stdio.h>

int main() {
    // Write C code here
    int n,i;
    scanf("%d",&n);
    int a[n];
    for( i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    int sno;
    scanf("%d",&sno);
    for(i=0;i<n;i++)
    {
        if(sno==a[i])
        {
            printf("element is found");
            break;
        }
    }
    if(i==n)
    {
        printf("elements not found");
    }
    return 0;
}
